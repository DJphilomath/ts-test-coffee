// require('ts-node/register');
const async = require('async');

module.exports = () => {
  // const { preprintServer } = require('./setup');
  // async.parallel([
  //   (pCb) => preprintServer.close(pCb),
  // ], () => {
    console.log("Goodbye!!!!!");
    process.exit();
  // })
};